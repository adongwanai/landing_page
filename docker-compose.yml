services:
  landing:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data
    env_file:
      - path: .env
        required: false  # local only; on Zeabur set vars in dashboard
    restart: unless-stopped
